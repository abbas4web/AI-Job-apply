// AI service — Gemini-powered generation tasks
// Called by BullMQ workers, not directly by controllers

export class AiService {
  // async generateCoverLetter(jobDescription: string, resume: string) {}
  // async tailorResume(jobDescription: string, resume: string) {}
  // async scoreJobMatch(jobDescription: string, resume: string) {}
}
