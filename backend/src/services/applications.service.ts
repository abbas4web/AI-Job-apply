// Applications service — implement business logic here
// e.g. status transitions, AI generation triggers

export class ApplicationsService {
  // async findAll(userId: string, filters: ApplicationFilters) {}
  // async findById(id: string, userId: string) {}
  // async create(userId: string, data: CreateApplicationDto) {}
  // async update(id: string, userId: string, data: UpdateApplicationDto) {}
  // async delete(id: string, userId: string) {}
}
